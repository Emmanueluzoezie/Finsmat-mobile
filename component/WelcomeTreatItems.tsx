import { ScrollView, StyleSheet, Text, View } from 'react-native'
import React from 'react'

const WelcomeTreatItems = () => {
  return (
      <ScrollView horizontal>
          {/* {item.content.map((subItem) => (
              <View key={subItem} style={{ width: 100, height: 100, margin: 10, backgroundColor: 'lightblue' }}>
                  <Text>{subItem}</Text>
              </View>
          ))} */}
          <Text>tetdy</Text>
      </ScrollView>
  );
}

export default WelcomeTreatItems

const styles = StyleSheet.create({})